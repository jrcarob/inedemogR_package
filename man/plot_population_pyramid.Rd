% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_demog.R
\name{plot_population_pyramid}
\alias{plot_population_pyramid}
\title{Plot a population pyramid}
\usage{
plot_population_pyramid(pop_df, year, region = NULL, title = NULL)
}
\arguments{
\item{pop_df}{Population tibble as returned by \code{get_ine_population()$data}
(columns \code{province_name}, \code{year}, \code{age}, \code{female}, \code{male}).}

\item{year}{Numeric, the year to plot.}

\item{region}{Optional character vector to subset by province name
(regular expressions allowed, matched against \code{province_name}). If
more than one province matches, counts are summed across them. If
\code{NULL} (default), all provinces are summed (national pyramid).}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a population pyramid for one province and year from
age-disaggregated population data. Requires an age/sex breakdown, so it
operates on \code{\link[=get_ine_population]{get_ine_population()}}'s output, not \code{\link[=get_ine_demog]{get_ine_demog()}}'s
\code{population_total} indicator.
}
\examples{
pop <- tibble::tibble(
  province_name = "A Coruna", year = 2023,
  age = c(0, 1), female = c(100, 90), male = c(105, 95)
)
plot_population_pyramid(pop, year = 2023)
}
