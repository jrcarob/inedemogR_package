% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deaths.R
\name{get_ine_deaths}
\alias{get_ine_deaths}
\title{Retrieve province-level deaths}
\usage{
get_ine_deaths(
  table_id = 6545,
  age_table_id = 6547,
  n_periods = 100,
  use_cache = TRUE,
  force = FALSE,
  cache_dir = tools::R_user_dir("inedemogR", "cache")
)
}
\arguments{
\item{table_id}{INE table ID for the age-less (province + sex) series.
Default \code{6545}, confirmed for "Defunciones por lugar de residencia y
sexo. Total nacional y provincias" (operation MNPD).}

\item{age_table_id}{INE table ID for the age-specific series. Default
\code{6547}.}

\item{n_periods}{Number of most recent years to fetch.}

\item{use_cache}{Logical (default \code{TRUE}). If a previous call already
cached a result for this \code{table_id}/\code{age_table_id}/\code{n_periods}
combination, return it directly instead of re-fetching from INE —
this function's live fetch (two tables) is one of the package's
slowest. Set \code{FALSE} to always hit the network.}

\item{force}{Logical (default \code{FALSE}). If \code{TRUE}, ignore any existing
cache and re-fetch from INE, refreshing the cache afterwards.}

\item{cache_dir}{Directory for cached data. Defaults to the same
location \code{\link[=update_ine_data]{update_ine_data()}} uses.}
}
\value{
\code{list(data_provinces, data_national, qc, qc_age)}:
\code{data_provinces} is age-specific (\code{ine_code}, \code{nuts3_code},
\code{nuts2_code}, \code{province_name}, \code{year}, \code{age}, \code{female}, \code{male},
\code{total}); \code{data_national} is Spain's age-less total.
}
\description{
Retrieves, cleans, and validates Spanish annual death counts by province
and sex from INE's Tempus3 JSON API, per the SHMD Protocol (Section 12,
Step 1: "Death Count Assembly"). Fetches both an age-less table (for the
national total and a national-vs-provincial reconciliation check) and an
age-specific table (needed by \code{\link[=compute_exposure]{compute_exposure()}} /
\code{\link[=compute_death_rates]{compute_death_rates()}}, which require D(x,t), not just D(t)).
}
\examples{
\dontrun{
# Not run: requires live network access to the INE API.
deaths <- get_ine_deaths(n_periods = 30)
deaths$data_provinces

# Re-fetch even if a cached copy exists:
deaths <- get_ine_deaths(n_periods = 30, force = TRUE)
}
}
