% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ine_geo.R
\name{get_ine_geo}
\alias{get_ine_geo}
\title{Get INE Geographic Data}
\usage{
get_ine_geo(
  geo_level = c("municipality", "province"),
  region = NULL,
  moveCAN = TRUE,
  can_gap_km = 60
)
}
\arguments{
\item{geo_level}{Character, one of \code{"municipality"} or \code{"province"}.}

\item{region}{Optional character vector to subset by region name
(regular expressions allowed).}

\item{moveCAN}{Logical, shift the Canary Islands next to the mainland for
compact national maps (default \code{TRUE}). Pass \code{FALSE} to keep their real
geographic coordinates.}

\item{can_gap_km}{Numeric, the gap in kilometers left between the shifted
Canary Islands and the closest point on the mainland coast (default
\code{60}). Only used when \code{moveCAN = TRUE}. Smaller values move the
islands closer.}
}
\value{
An \code{sf} object with columns \code{GEOID}, \code{NAME}, and \code{geometry},
projected to ETRS89 / UTM zone 30N (EPSG:25830). When \code{moveCAN = TRUE}
and the shift was applied, an sfc rectangle marking the shifted
islands' extent is attached as the \code{"can_box"} attribute (used by
\code{\link[=plot_ine_map]{plot_ine_map()}} to draw an inset frame).
}
\description{
Retrieves spatial geometries for INE geographic levels via the
\href{https://ropenspain.github.io/mapSpain/}{mapSpain} package, which sources
boundaries from Instituto Geografico Nacional / GISCO. Geometries are
returned with a \code{GEOID} column that matches the geographic codes returned
by \code{\link[=get_ine_demog]{get_ine_demog()}}, so the two can be joined directly.
}
\examples{
\dontrun{
# Not run: requires live network access to fetch boundaries via mapSpain.
prov_geo <- get_ine_geo(geo_level = "province")
mun_geo <- get_ine_geo(geo_level = "municipality", region = "Sevilla")
}
}
