% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{sex_ratio}
\alias{sex_ratio}
\title{Sex ratio}
\usage{
sex_ratio(pop_df, by_age = FALSE)
}
\arguments{
\item{pop_df}{Population tibble as returned by \code{get_ine_population()$data}
(columns \code{nuts3_code}, \code{province_name}, \code{year}, \code{age}, \code{female}, \code{male}).}

\item{by_age}{Logical, keep the \code{age} column and compute a sex ratio per
age (default \code{FALSE}, which aggregates over all ages first).}
}
\value{
A tibble with \code{nuts3_code}, \code{province_name}, \code{year} (and \code{age} if
\code{by_age = TRUE}), \code{sex_ratio}.
}
\description{
Computes the sex ratio (males per 100 females) per province and year
from age-disaggregated population data. Requires the \code{female}/\code{male}
columns \code{\link[=get_ine_population]{get_ine_population()}} provides; \code{\link[=get_ine_demog]{get_ine_demog()}}'s
\code{population_total} indicator has no sex breakdown and cannot be used
here.
}
\examples{
pop <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna", year = 2023,
  age = c(0, 1), female = c(100, 95), male = c(105, 100)
)
sex_ratio(pop)
sex_ratio(pop, by_age = TRUE)
}
