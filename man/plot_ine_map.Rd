% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ine_map.R
\name{plot_ine_map}
\alias{plot_ine_map}
\title{Plot a Map of Spain with Regions Highlighted}
\usage{
plot_ine_map(
  geo_level = c("province", "municipality"),
  highlight = NULL,
  base_color = "grey90",
  highlight_color = "firebrick",
  title = NULL,
  can_box = TRUE,
  can_gap_km = 60
)
}
\arguments{
\item{geo_level}{Character, one of \code{"province"} or \code{"municipality"}.}

\item{highlight}{Character vector of region names to highlight (regular
expressions allowed, matched against the geography name — same
matching rules as \code{region} in \code{\link[=get_ine_demog]{get_ine_demog()}}/\code{\link[=get_ine_geo]{get_ine_geo()}}).
If \code{NULL} (default), the whole map is plotted in \code{base_color} with no
highlighting.}

\item{base_color}{Fill color for non-highlighted regions. Default
\code{"grey90"}.}

\item{highlight_color}{Fill color for highlighted regions. Default
\code{"firebrick"}.}

\item{title}{Optional plot title.}

\item{can_box}{Logical, draw an inset separator box around the Canary
Islands, which are shifted next to the mainland for a compact map
(default \code{TRUE}).}

\item{can_gap_km}{Numeric, how close (in km) the shifted Canary Islands
sit next to the mainland (default \code{60}). Smaller values move them
closer. See \code{\link[=get_ine_geo]{get_ine_geo()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the full map of Spain at the requested geographic level (provinces
or municipalities), with one or more regions highlighted in a different
color. Useful for confirming \emph{which} regions a \code{region} filter passed to
\code{\link[=get_ine_demog]{get_ine_demog()}} or \code{\link[=get_ine_geo]{get_ine_geo()}} actually matches, since
\code{\link[=get_ine_geo]{get_ine_geo()}} uses regex substring matching by default (see
\code{vignette} examples in \code{test_cordoba.R}).
}
\examples{
\dontrun{
# Not run: get_ine_geo() requires live network access to fetch boundaries.
plot_ine_map(geo_level = "province", highlight = "^Córdoba$")
plot_ine_map(geo_level = "municipality", highlight = "Córdoba")
}
}
