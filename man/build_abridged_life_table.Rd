% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abridged_life_tables.R
\name{build_abridged_life_table}
\alias{build_abridged_life_table}
\title{Build an abridged (5-year age group) period life table}
\usage{
build_abridged_life_table(mx_1x1, mx_5x1, sex = c("female", "male"))
}
\arguments{
\item{mx_1x1}{tibble with columns \code{age} (single years, at least
\code{0:4}), \code{mx}, for one year and one sex - see \code{\link[=compute_death_rates]{compute_death_rates()}}'s
\code{mx_1x1} output, filtered to one province/year/sex.}

\item{mx_5x1}{tibble with columns \code{age_group} (\code{"00-04"}, \code{"05-09"},
..., \code{"100+"}), \code{mx}, for the same year and sex - see
\code{\link[=compute_death_rates]{compute_death_rates()}}'s \code{mx_5x1} output, filtered the same way.}

\item{sex}{\code{"female"} or \code{"male"} - selects the Andreev-Kingkade a0
branch for the youngest group, same as \code{\link[=build_life_table]{build_life_table()}}.}
}
\value{
tibble: \code{age_group}, \code{age_start}, \code{n} (interval width, \code{NA}
for the open interval), \code{mx}, \code{ax}, \code{qx}, \code{lx}, \code{dx}, \code{Lx}, \code{Tx}, \code{ex}.
}
\description{
Constructs a full abridged period life table (age groups \code{"00-04"},
\code{"05-09"}, ..., \code{"95-99"}, \code{"100+"}) directly from grouped central
death rates, per the standard discrete abridged life-table method
(Chiang's \code{nqx} formula; Preston, Heuveline & Guillot 2001, Ch. 3).
Complements \code{\link[=build_life_table]{build_life_table()}}'s single-year (1x1) tables - use
this when comparing against other agencies' published abridged
tables, or when only grouped-age data is available. The youngest
group (\code{"00-04"}) is the one exception to using \code{mx_5x1} alone: it is
built from an exact single-year sub-table using \code{mx_1x1}'s
Andreev-Kingkade \code{a0} (see \code{\link[=andreev_kingkade_a0]{andreev_kingkade_a0()}}), avoiding the
external nax(0-4) regression a from-mx_5x1-alone method would
otherwise need for that interval's sharp infant-mortality gradient.
}
\examples{
mx1 <- data.frame(age = 0:4, mx = c(0.004, 0.0003, 0.0002, 0.0002, 0.0002))
mx5 <- data.frame(
  age_group = c("00-04", "05-09", "100+"), mx = c(0.0006, 0.0001, 0.35)
)
build_abridged_life_table(mx1, mx5, "female")
}
