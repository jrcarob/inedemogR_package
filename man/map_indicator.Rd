% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_demog.R
\name{map_indicator}
\alias{map_indicator}
\title{Map any indicator by province or municipality}
\usage{
map_indicator(
  df,
  value_col,
  geo_level = c("province", "municipality"),
  region = NULL,
  binned = TRUE,
  breaks = NULL,
  palette = if (binned) "PiYG" else "D",
  moveCAN = TRUE,
  can_gap_km = 60,
  legend_title = NULL,
  title = NULL
)
}
\arguments{
\item{df}{A tibble with a \code{GEOID} column (matching \code{\link[=get_ine_geo]{get_ine_geo()}}'s
\code{GEOID} for the requested \code{geo_level}) and \code{value_col}.}

\item{value_col}{Character, the name of the numeric column to map.}

\item{geo_level}{Character, one of \code{"province"} or \code{"municipality"}.}

\item{region}{Optional character vector to subset the geometry by
region name (regular expressions allowed). See \code{\link[=get_ine_geo]{get_ine_geo()}}.}

\item{binned}{Logical (default \code{TRUE}). If \code{TRUE}, uses a discrete,
binned color scale (\code{ggplot2::scale_fill_fermenter()}) — a fixed
number of color bands with a legend showing each band's range,
matching the classic choropleth style. If \code{FALSE}, uses a continuous
gradient (\code{ggplot2::scale_fill_viridis_c()} by default).}

\item{breaks}{Numeric vector of bin edges when \code{binned = TRUE}. If
\code{NULL} (default), computed automatically via \code{pretty()}.}

\item{palette}{Character, a \code{RColorBrewer} palette name when
\code{binned = TRUE} (default \code{"PiYG"}, a pink-to-green diverging
palette well suited to ratios centered on 1) or a \code{viridis} option
letter when \code{binned = FALSE} (default \code{"D"}).}

\item{moveCAN}{Logical, shift the Canary Islands next to the mainland
(default \code{TRUE}). See \code{\link[=get_ine_geo]{get_ine_geo()}}.}

\item{can_gap_km}{Numeric, gap in km for the shifted Canary Islands
(default \code{60}). See \code{\link[=get_ine_geo]{get_ine_geo()}}.}

\item{legend_title}{Optional legend title (defaults to \code{value_col}).}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
General-purpose choropleth: joins a tidy indicator tibble (keyed by
\code{GEOID}, matching \code{\link[=get_ine_geo]{get_ine_geo()}}'s output — e.g. \code{\link[=get_ine_demog]{get_ine_demog()}}'s
output already has it) onto province or municipality geometry and
renders it, either as a binned/discrete-legend map (the default,
matching the classic "N provinces above/below a threshold" style of
choropleth) or a continuous gradient. \code{\link[=map_life_expectancy]{map_life_expectancy()}} is a
thin wrapper around this function for System B's life-table output.
}
\examples{
\dontrun{
# Not run: get_ine_geo() requires live network access to fetch boundaries.
pop_data <- get_ine_demog(indicator = "population_total", year = 2023)
map_indicator(pop_data, "population_total", geo_level = "province")
}
}
