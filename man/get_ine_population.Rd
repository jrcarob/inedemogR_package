% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{get_ine_population}
\alias{get_ine_population}
\title{Retrieve province-level population by age and sex}
\usage{
get_ine_population(
  table_id = 56945,
  n_periods = 30,
  use_cache = TRUE,
  force = FALSE,
  cache_dir = tools::R_user_dir("inedemogR", "cache")
)
}
\arguments{
\item{table_id}{INE table ID. Default \code{56945} ("Poblacion residente por
fecha, sexo y edad (desde 1971)", provincial variant, operation ECP).
Pass \code{NULL} to auto-discover.}

\item{n_periods}{Number of most recent years to fetch.}

\item{use_cache}{Logical (default \code{TRUE}). If a previous call already
cached a result for this \code{table_id}/\code{n_periods} combination, return
it directly instead of re-fetching from INE, since this function's live
fetch (often via the slower CSV bulk-export fallback) is one of the
package's slowest. Set \code{FALSE} to always hit the network.}

\item{force}{Logical (default \code{FALSE}). If \code{TRUE}, ignore any existing
cache and re-fetch from INE, refreshing the cache afterwards.}

\item{cache_dir}{Directory for cached data. Defaults to the same
location \code{\link[=update_ine_data]{update_ine_data()}} uses.}
}
\value{
\code{list(data, qc)}: \code{data} is a tibble with columns \code{ine_code},
\code{nuts3_code}, \code{nuts2_code}, \code{province_name}, \code{year}, \code{age}, \code{female},
\code{male}, \code{total}; \code{qc} is the output of \code{\link[=validate_population]{validate_population()}}.
}
\description{
Retrieves, cleans, and validates Spanish January-1st provincial
population estimates by single-year age and sex from INE's Padron
Municipal Continuo, per the SHMD Protocol (Section 12, Step 3:
"Population Estimates"). Covers 1996-present; pre-1996 years require the
intercensal survival method applied to the 1981/1991 censuses, which is
out of scope here.
}
\details{
Some province x age x sex population tables are too large for INE's JSON
API and are rejected outright; this function automatically falls back to
INE's full CSV bulk export in that case (a larger download, but with no
series-count limit).
}
\examples{
\dontrun{
# Not run: requires live network access to the INE API.
pop <- get_ine_population(n_periods = 10)
pop$data

# Re-fetch even if a cached copy exists:
pop <- get_ine_population(n_periods = 10, force = TRUE)
}
}
