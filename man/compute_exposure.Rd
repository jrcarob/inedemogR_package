% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{compute_exposure}
\alias{compute_exposure}
\title{Compute exposure-to-risk for every province}
\usage{
compute_exposure(population, deaths)
}
\arguments{
\item{population}{Output of the \code{data} element of \code{\link[=get_ine_population]{get_ine_population()}}.}

\item{deaths}{Output of the \code{data_provinces} element of \code{\link[=get_ine_deaths]{get_ine_deaths()}}.}
}
\value{
\code{list(data, qc)}: \code{data} is a tibble with columns \code{nuts3_code},
\code{province_name}, \code{year}, \code{age}, \code{female}, \code{male}, \code{total} (exposure),
\code{is_boundary_year}, \code{is_missing_source_pop}; \code{qc} is the output of
\code{\link[=validate_exposure]{validate_exposure()}}.
}
\description{
Computes E(x,t) = 1/2[P(x,t) + P(x,t+1)] + 1/2[D_upper(x,t) -
D_lower(x,t)] for every province present in both \code{population} and
\code{deaths}, per SHMD Protocol Section 12, Step 4. INE's Tempus3 death
tables don't carry a birth-cohort split, so the Lexis triangle is
derived via the documented 50/50 fallback (HMD Methods Protocol V6
Appendix A) unless \code{deaths} already carries a \code{cohort} column.
}
\details{
Death registration lags population estimates by about a year, so
\code{population} commonly includes a most-recent year (e.g. a Jan-1 stock
snapshot) with no corresponding rows anywhere in \code{deaths} yet. That
year is still used as the \code{P(x,t+1)} boundary for computing the
\emph{previous} year's exposure, but is \strong{not} included in the returned
exposure rows itself — treating an entirely-absent year as zero deaths
(rather than missing data) would mechanically produce zero mortality
for that year, and downstream a nonsensical inflated life expectancy.
}
\examples{
population <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna",
  year = c(2022, 2023), age = 0,
  female = c(100, 105), male = c(105, 110), total = c(205, 215)
)
deaths <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna",
  year = c(2022, 2023), age = 0,
  female = c(1, 1), male = c(1, 2), total = c(2, 3)
)
compute_exposure(population, deaths)
}
