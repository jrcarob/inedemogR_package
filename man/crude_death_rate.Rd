% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{crude_death_rate}
\alias{crude_death_rate}
\title{Crude death rate}
\usage{
crude_death_rate(deaths_df, pop_df)
}
\arguments{
\item{deaths_df}{Deaths tibble as returned by \code{get_ine_deaths() $data_provinces} (columns \code{nuts3_code}, \code{province_name}, \code{year},
\code{age}, \code{total}). Unlike \code{\link[=crude_birth_rate]{crude_birth_rate()}}'s \code{births_df}, this is
age-specific, so it is summed over age internally before computing
the rate.}

\item{pop_df}{Population tibble as returned by \code{get_ine_population()$data}
(columns \code{nuts3_code}, \code{year}, \code{age}, \code{total}).}
}
\value{
A tibble with \code{nuts3_code}, \code{province_name}, \code{year}, \code{cdr}.
}
\description{
Computes the crude death rate (deaths per 1,000 population) per
province and year, joining \code{\link[=get_ine_deaths]{get_ine_deaths()}} and \code{\link[=get_ine_population]{get_ine_population()}}
output. The symmetric counterpart to \code{\link[=crude_birth_rate]{crude_birth_rate()}} — see
\code{\link[=rate_of_natural_increase]{rate_of_natural_increase()}} to combine the two.
}
\examples{
deaths <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna", year = 2023,
  age = c(0, 1), total = c(1, 49)
)
pop <- tibble::tibble(
  nuts3_code = "ES111", year = 2023, age = c(0, 1), total = c(5000, 5000)
)
crude_death_rate(deaths, pop)
}
