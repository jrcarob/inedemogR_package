% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{net_reproduction_rate}
\alias{net_reproduction_rate}
\title{Net reproduction rate (NRR)}
\usage{
net_reproduction_rate(asfr_df, lt_df)
}
\arguments{
\item{asfr_df}{Output of \code{\link[=age_specific_fertility_rate]{age_specific_fertility_rate()}}.}

\item{lt_df}{\code{build_life_tables()}'s \code{fltper} tibble (columns
\code{nuts3_code}, \code{year}, \code{age}, \code{Lx}) - must be the \emph{female} life table,
since NRR tracks mothers' own survival.}
}
\value{
A tibble with \code{nuts3_code}, \code{province_name}, \code{year}, \code{nrr}.
}
\description{
Computes the net reproduction rate (expected daughters per woman over
her reproductive lifetime, accounting for the mother's own survival to
each reproductive age) per province and year, weighting
\code{\link[=age_specific_fertility_rate]{age_specific_fertility_rate()}}'s \code{asfr_female} by the female
life-table survivorship function \code{Lx} from \code{\link[=build_life_tables]{build_life_tables()}}'s
\code{fltper} (person-years lived in the age interval, per HMD Methods
Protocol V6 - the same \code{Lx} \code{\link[=life_expectancy_summary]{life_expectancy_summary()}}'s \code{ex} is
derived from). Unlike \code{\link[=gross_reproduction_rate]{gross_reproduction_rate()}}, which assumes every
woman survives to bear children at every age, NRR is the mortality-
adjusted figure: NRR < GRR whenever there is any mortality before the
end of the reproductive span.
}
\examples{
asfr <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna", year = 2023,
  age = c(20, 30), asfr = c(30, 80), asfr_female = c(15, 40)
)
fltper <- tibble::tibble(
  nuts3_code = "ES111", year = 2023, age = c(20, 30), Lx = c(99000, 98500)
)
net_reproduction_rate(asfr, fltper)
}
