% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_demog.R
\name{map_life_expectancy}
\alias{map_life_expectancy}
\title{Map life expectancy by province}
\usage{
map_life_expectancy(
  le_df,
  year,
  sex = "both",
  moveCAN = TRUE,
  can_gap_km = 60,
  title = NULL
)
}
\arguments{
\item{le_df}{Output of \code{\link[=life_expectancy_summary]{life_expectancy_summary()}} (columns
\code{nuts3_code}, \code{year}, \code{e0}, \code{sex}).}

\item{year}{Numeric, the year to plot.}

\item{sex}{Character, one of \code{"female"}, \code{"male"}, \code{"both"}, matching
the \code{sex} value in \code{le_df} to plot.}

\item{moveCAN}{Logical, shift the Canary Islands next to the mainland
(default \code{TRUE}). See \code{\link[=get_ine_geo]{get_ine_geo()}}.}

\item{can_gap_km}{Numeric, gap in km for the shifted Canary Islands
(default \code{60}). See \code{\link[=get_ine_geo]{get_ine_geo()}}.}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Bridges System B's mortality pipeline (\code{\link[=build_life_tables]{build_life_tables()}} via
\code{\link[=life_expectancy_summary]{life_expectancy_summary()}}) with System A's spatial layer
(\code{\link[=get_ine_geo]{get_ine_geo()}}) to render a province-level choropleth of life
expectancy at birth. A thin wrapper around \code{\link[=map_indicator]{map_indicator()}} (a
continuous gradient, since life expectancy has no natural discrete
bins) that handles the \code{nuts3_code} -> \code{GEOID} bridge via
\code{province_lookup}. See \code{\link[=plot_ine_map]{plot_ine_map()}} for the analogous
region-highlighting map built purely on \code{\link[=get_ine_geo]{get_ine_geo()}}.
}
\examples{
\dontrun{
# Not run: get_ine_geo() requires live network access to fetch province
# boundaries.
lt <- build_life_tables(rates$mx_1x1)
le <- life_expectancy_summary(lt$fltper, sex = "female")
map_life_expectancy(le, year = max(le$year), sex = "female")
}
}
