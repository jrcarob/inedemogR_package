% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/births.R
\name{get_ine_births_by_age}
\alias{get_ine_births_by_age}
\title{Retrieve province-level births by age of mother}
\usage{
get_ine_births_by_age(
  table_id = 6508,
  n_periods = 100,
  use_cache = TRUE,
  force = FALSE,
  cache_dir = tools::R_user_dir("inedemogR", "cache")
)
}
\arguments{
\item{table_id}{INE Tempus3 table ID. Default \code{6508}, confirmed for
"Nacimientos por lugar de residencia de la madre, sexo y edad de la
madre. Total nacional y provincias" (operation MNPN).}

\item{n_periods}{Number of most recent years to fetch. This table has
~6,000 series (province x age x newborn-sex), so fetches are slower
than \code{\link[=get_ine_births]{get_ine_births()}}'s; caching (see \code{use_cache}) matters more here.}

\item{use_cache}{Logical (default \code{TRUE}). See \code{\link[=get_ine_births]{get_ine_births()}}.}

\item{force}{Logical (default \code{FALSE}). See \code{\link[=get_ine_births]{get_ine_births()}}.}

\item{cache_dir}{Directory for cached data. Defaults to the same
location \code{\link[=update_ine_data]{update_ine_data()}} uses.}
}
\value{
\code{list(data, qc)}: \code{data} is a tibble with columns \code{ine_code},
\code{nuts3_code}, \code{nuts2_code}, \code{province_name}, \code{year}, \code{age} (integer,
\code{NA} for the \code{under15} group), \code{age_group} (character: \code{"under15"},
\code{"15"}..\code{"49"}, \code{"50plus"}), \code{female}, \code{male}, \code{total} (births to
mothers of that age, by sex of the newborn); \code{qc} is the output of
\code{\link[=validate_births_by_age]{validate_births_by_age()}}.
}
\description{
Retrieves, cleans, and validates Spanish annual live-birth counts by
province, single year of age of the mother (15-49, plus the two open
intervals \code{under15}/\verb{50plus}), and sex of the newborn, from INE's raw
Tempus3 JSON API. Unlike \code{\link[=get_ine_births]{get_ine_births()}} (which has no age-of-mother
breakdown), this is the data source needed for age-specific fertility
rates - see \code{\link[=age_specific_fertility_rate]{age_specific_fertility_rate()}}, \code{\link[=total_fertility_rate]{total_fertility_rate()}},
\code{\link[=mean_age_at_childbearing]{mean_age_at_childbearing()}}, \code{\link[=gross_reproduction_rate]{gross_reproduction_rate()}}, and
\code{\link[=net_reproduction_rate]{net_reproduction_rate()}}.
}
\examples{
\dontrun{
# Not run: requires live network access to the INE API.
births_age <- get_ine_births_by_age(n_periods = 10)
births_age$data
}
}
