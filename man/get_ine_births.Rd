% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/births.R
\name{get_ine_births}
\alias{get_ine_births}
\title{Retrieve province-level births}
\usage{
get_ine_births(
  table_id = 6506,
  n_periods = 100,
  use_cache = TRUE,
  force = FALSE,
  cache_dir = tools::R_user_dir("inedemogR", "cache")
)
}
\arguments{
\item{table_id}{INE Tempus3 table ID. Default \code{6506}, confirmed for
"Nacimientos por lugar de residencia de la madre y sexo. Total nacional
y provincias" (operation MNPN). Pass \code{NULL} to auto-discover.}

\item{n_periods}{Number of most recent years to fetch.}

\item{use_cache}{Logical (default \code{TRUE}). If a previous call already
cached a result for this \code{table_id}/\code{n_periods} combination, return
it directly instead of re-fetching from INE — this function's live
fetch is one of the package's slowest. Set \code{FALSE} to always hit the
network.}

\item{force}{Logical (default \code{FALSE}). If \code{TRUE}, ignore any existing
cache and re-fetch from INE, refreshing the cache afterwards.}

\item{cache_dir}{Directory for cached data. Defaults to the same
location \code{\link[=update_ine_data]{update_ine_data()}} uses.}
}
\value{
\code{list(data, qc)}: \code{data} is a tibble with columns \code{ine_code},
\code{nuts3_code}, \code{nuts2_code}, \code{province_name}, \code{year}, \code{female}, \code{male},
\code{total}; \code{qc} is the output of \code{\link[=validate_births]{validate_births()}}.
}
\description{
Retrieves, cleans, and validates Spanish annual live-birth counts by
province and sex from INE's Tempus3 JSON API, per the SHMD Protocol
(Section 12, Step 1: "Birth Count Assembly"). Unlike \code{\link[=get_ine_demog]{get_ine_demog()}}
(which returns a \code{Total}-sex-only province count), this returns
sex-disaggregated counts joined to NUTS-3 codes, suitable as an
HMD-format Births input file - see \code{\link[=download_ine_data]{download_ine_data()}} to write it to disk.
}
\examples{
\dontrun{
# Not run: requires live network access to the INE API.
births <- get_ine_births(n_periods = 30)
births$data
births$qc$issues

# Re-fetch even if a cached copy exists:
births <- get_ine_births(n_periods = 30, force = TRUE)
}
}
