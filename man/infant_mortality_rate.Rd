% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{infant_mortality_rate}
\alias{infant_mortality_rate}
\title{Infant mortality rate}
\usage{
infant_mortality_rate(deaths_df, births_df)
}
\arguments{
\item{deaths_df}{Deaths tibble as returned by \code{get_ine_deaths() $data_provinces} (columns \code{nuts3_code}, \code{province_name}, \code{year},
\code{age}, \code{total}).}

\item{births_df}{Births tibble as returned by \code{get_ine_births()$data}
(columns \code{nuts3_code}, \code{year}, \code{total}).}
}
\value{
A tibble with \code{nuts3_code}, \code{province_name}, \code{year}, \code{imr}.
}
\description{
Computes the infant mortality rate (deaths under age 1 per 1,000 live
births) per province and year, joining \code{\link[=get_ine_deaths]{get_ine_deaths()}} and
\code{\link[=get_ine_births]{get_ine_births()}} output.
}
\details{
This is the standard demographic IMR: infant deaths in a year divided
by live births \emph{in that same year} (the birth cohort those deaths are
drawn from), not by population — a different denominator convention
from \code{\link[=crude_death_rate]{crude_death_rate()}}. It is closely related to, but not
identical to, \code{q0} (probability of dying before age 1) in
\code{\link[=build_life_table]{build_life_table()}}'s output: \code{q0} is derived from the exposure-based
central death rate \code{m0} via the Andreev-Kingkade \code{a0} (per HMD Methods
Protocol V6), while IMR here uses the simpler, more standard
deaths/births ratio directly. The two will usually be close but need
not match exactly.
}
\examples{
deaths <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna", year = 2023,
  age = c(0, 1, 65), total = c(3, 1, 40)
)
births <- tibble::tibble(nuts3_code = "ES111", year = 2023, total = 1000)
infant_mortality_rate(deaths, births)
}
