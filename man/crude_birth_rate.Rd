% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{crude_birth_rate}
\alias{crude_birth_rate}
\title{Crude birth rate}
\usage{
crude_birth_rate(births_df, pop_df)
}
\arguments{
\item{births_df}{Births tibble as returned by \code{get_ine_births()$data}
(columns \code{nuts3_code}, \code{province_name}, \code{year}, \code{total}).}

\item{pop_df}{Population tibble as returned by \code{get_ine_population()$data}
(columns \code{nuts3_code}, \code{year}, \code{age}, \code{total}).}
}
\value{
A tibble with \code{nuts3_code}, \code{province_name}, \code{year}, \code{cbr}.
}
\description{
Computes the crude birth rate (births per 1,000 population) per province
and year, joining \code{\link[=get_ine_births]{get_ine_births()}} and \code{\link[=get_ine_population]{get_ine_population()}} output.
}
\details{
This is a \emph{crude} birth rate (total births / total population), \strong{not}
a total fertility rate (TFR). \code{\link[=get_ine_births]{get_ine_births()}} itself has no
age-of-mother breakdown, so \code{crude_birth_rate()} cannot compute a TFR
on its own — for a true TFR, use \code{\link[=get_ine_births_by_age]{get_ine_births_by_age()}} with
\code{\link[=age_specific_fertility_rate]{age_specific_fertility_rate()}}/\code{\link[=total_fertility_rate]{total_fertility_rate()}} instead. Do not
substitute \code{crude_birth_rate()} for TFR in fertility analysis.
}
\examples{
births <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna", year = 2023, total = 100
)
pop <- tibble::tibble(
  nuts3_code = "ES111", year = 2023, age = c(0, 1), total = c(5000, 5000)
)
crude_birth_rate(births, pop)
}
