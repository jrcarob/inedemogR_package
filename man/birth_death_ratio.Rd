% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{birth_death_ratio}
\alias{birth_death_ratio}
\title{Birth-to-death ratio}
\usage{
birth_death_ratio(df)
}
\arguments{
\item{df}{Output of \code{get_ine_demog(indicator = c("births_total", "deaths_total"))} (columns \code{GEOID}, \code{NAME}, \code{year}, \code{births_total},
\code{deaths_total}).}
}
\value{
A tibble with \code{GEOID}, \code{NAME}, \code{year}, \code{birth_death_ratio}. A
value of \code{2} means 2 live births per death; \code{0.5} means 1 birth per
2 deaths.
}
\description{
Computes the birth-to-death ratio (live births per death) per
geography and year. Unlike \code{\link[=age_dependency_ratio]{age_dependency_ratio()}}/\code{\link[=aging_index]{aging_index()}}/
\code{\link[=sex_ratio]{sex_ratio()}}, this needs no age breakdown, so it operates directly on
\code{\link[=get_ine_demog]{get_ine_demog()}}'s totals (System A) rather than \code{\link[=get_ine_population]{get_ine_population()}}
(System B) — fetch both \code{births_total} and \code{deaths_total} in one call
and pass the result straight through.
}
\examples{
df <- tibble::tibble(
  GEOID = c("15", "28"), NAME = c("A Coruna", "Madrid"), year = 2023,
  births_total = c(3000, 30000), deaths_total = c(6000, 25000)
)
birth_death_ratio(df)
}
