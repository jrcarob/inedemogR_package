% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ine_demog.R
\name{get_ine_demog}
\alias{get_ine_demog}
\title{Get INE Demographic Data}
\usage{
get_ine_demog(
  indicator,
  geo_level = NULL,
  year = NULL,
  region = NULL,
  sex = "Total",
  geometry = FALSE
)
}
\arguments{
\item{indicator}{Character vector of indicator codes (see
\code{\link[=list_ine_indicators]{list_ine_indicators()}}).}

\item{geo_level}{Character, the geographic level to fetch. Must match the
level at which every requested indicator is actually published (see
\code{\link[=list_ine_indicators]{list_ine_indicators()}}). If \code{NULL} (default), inferred from \code{indicator}.}

\item{year}{Numeric or character, the year of data. Default (\code{NULL})
returns the latest available period only.}

\item{region}{Optional character vector to subset by region name
(regular expressions allowed, matched against the geography name).}

\item{sex}{Character, one of \code{"Total"}, \code{"Hombres"}, \code{"Mujeres"}.}

\item{geometry}{Logical, join geometries from \code{\link[=get_ine_geo]{get_ine_geo()}} (default
\code{FALSE}).}
}
\value{
A tidy tibble with columns \code{GEOID}, \code{NAME}, \code{year}, and one column
per requested indicator; an \code{sf} object if \code{geometry = TRUE}.
}
\description{
Retrieves demographic data from INE via the official
\code{\link[ineapir:get_data_table]{ineapir::get_data_table()}} API wrapper, and tidies it into a data frame
with one row per geography x year and one column per indicator.
}
\details{
Each \code{indicator} is mapped to a real INE table id via the \link{ine_variables}
registry (see \code{list_ine_indicators()}). Different indicators are only
available at different geographic granularities in INE's source tables
(e.g. population is published by municipality, but births/deaths only
down to province) — all requested indicators must share the same
\code{geo_level}, or request them separately.
}
\examples{
\dontrun{
# Not run: requires live network access to the INE API.
pop_data <- get_ine_demog(indicator = "population_total", year = 2023)
births_deaths <- get_ine_demog(
  indicator = c("births_total", "deaths_total"), year = 2023
)
}
}
