% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators.R
\name{general_fertility_rate}
\alias{general_fertility_rate}
\title{General fertility rate}
\usage{
general_fertility_rate(births_df, pop_df, age_min = 15L, age_max = 49L)
}
\arguments{
\item{births_df}{Births tibble as returned by \code{get_ine_births()$data}
(columns \code{nuts3_code}, \code{province_name}, \code{year}, \code{total}).}

\item{pop_df}{Population tibble as returned by \code{get_ine_population()$data}
(columns \code{nuts3_code}, \code{year}, \code{age}, \code{female}).}

\item{age_min}{Integer, the youngest age counted as reproductive-age
(default \code{15}).}

\item{age_max}{Integer, the oldest age counted as reproductive-age
(default \code{49}).}
}
\value{
A tibble with \code{nuts3_code}, \code{province_name}, \code{year}, \code{gfr}.
}
\description{
Computes the general fertility rate (births per 1,000 women of
reproductive age) per province and year, joining \code{\link[=get_ine_births]{get_ine_births()}}
and \code{\link[=get_ine_population]{get_ine_population()}} output.
}
\details{
GFR improves on \code{\link[=crude_birth_rate]{crude_birth_rate()}} by dividing births by the female
population actually at risk of childbearing (age \code{age_min}-\code{age_max},
default 15-49) instead of the total population — this removes the
distortion \code{\link[=crude_birth_rate]{crude_birth_rate()}} suffers when two provinces have
different age/sex structures (e.g. one with proportionally more
elderly residents) despite similar underlying fertility behavior.

GFR is still \strong{not} a total fertility rate (TFR): it is a single
aggregate rate over all reproductive-age women, not an age-specific
schedule. For a true TFR, use \code{\link[=get_ine_births_by_age]{get_ine_births_by_age()}} with
\code{\link[=age_specific_fertility_rate]{age_specific_fertility_rate()}}/\code{\link[=total_fertility_rate]{total_fertility_rate()}} instead.
}
\examples{
births <- tibble::tibble(
  nuts3_code = "ES111", province_name = "A Coruna", year = 2023, total = 100
)
pop <- tibble::tibble(
  nuts3_code = "ES111", year = 2023, age = c(10, 25, 40, 60),
  female = c(2000, 2500, 2500, 3000)
)
general_fertility_rate(births, pop)
}
