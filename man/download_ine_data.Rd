% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_ine_data.R
\name{download_ine_data}
\alias{download_ine_data}
\title{Download SHMD pipeline data to a local folder}
\usage{
download_ine_data(
  out_dir,
  stages = c("births", "deaths", "population", "exposure", "mx", "life_tables"),
  format = c("csv", "txt"),
  n_periods = 30
)
}
\arguments{
\item{out_dir}{Directory to write into (created if it doesn't exist).}

\item{stages}{Character vector of stages to include. One or more of
\code{"births"}, \code{"deaths"}, \code{"population"}, \code{"exposure"}, \code{"mx"},
\code{"life_tables"}. Default: all of them.}

\item{format}{Character vector, one or both of \code{"csv"} and \code{"txt"}.
\code{"csv"} writes one combined file per data table; \code{"txt"} writes
HMD-format files, one per province. Default: both.}

\item{n_periods}{Number of most recent years to fetch for the
births/deaths/population retrieval stages.}
}
\value{
Invisibly, a list with the in-memory results of every stage that
was run (\code{births}, \code{deaths}, \code{population}, \code{exposure}, \code{death_rates},
\code{life_tables}, each \code{NULL} if not run) and \code{files}, a character vector
of every path written.
}
\description{
Runs the requested stage(s) of the SHMD (Spanish subnational Human
Mortality Database) protocol pipeline — retrieval of province-level
births/deaths/population from INE, exposure-to-risk, central death
rates, and period life tables — and writes the results into \code{out_dir}
as \code{.csv} and/or HMD-format \code{.txt} files, so they can be inspected,
shared, or fed into other HMD-compatible tooling without staying in the
R session.
}
\details{
Later stages depend on earlier ones (\code{exposure} needs \code{population} and
\code{deaths}; \code{mx} needs \code{deaths} and \code{exposure}; \code{life_tables} needs \code{mx}).
Requesting a later stage automatically fetches/computes and writes its
dependencies too, since they were already retrieved along the way.
}
\examples{
\dontrun{
# Not run: requires live network access to the INE API, and writes files
# to disk.
# Everything, both formats, into ./ine_data
download_ine_data("ine_data")

# Just births and deaths, CSV only
download_ine_data("ine_data", stages = c("births", "deaths"), format = "csv")

# Full pipeline through life tables
download_ine_data("ine_data", stages = "life_tables", n_periods = 40)
}
}
